Stream消息接收者
    在同一个分组内的服务消息接收者，存在竞争关系，只有一个能接收并处理消息
    在不同分组的服务消息接收者，不存在竞争关系，能同时接收到并处理消息（默认不同分组）

    自定义配置分组：yaml配置

    持久化：重启消费者服务器，仍然能够接收到，在关闭期间，消息发送者的消息（主动获取）
        去掉分组后的服务器重启后，不能获取（信息丢失）
        但保留分组的服务器重启后，能获取


